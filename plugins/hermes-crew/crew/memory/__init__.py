"""Keeping a teammate's standing rules usable.

Two halves. :mod:`crew.memory.filters` decides what must never be written
down; :mod:`crew.memory.gardener` keeps what *is* written down from reaching
the ceiling where the store stops accepting anything new.
"""
